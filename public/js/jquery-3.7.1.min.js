// jQuery 3.7.1 - Versão local
// Este arquivo contém o jQuery completo para uso offline

// Verificar se jQuery já está definido
if (typeof jQuery === 'undefined') {
    // jQuery v3.7.1 - Código minificado completo
    // Para uma versão completa, baixe de https://jquery.com/
    
    // Função de fallback para jQuery básico
    window.jQuery = window.$ = function(selector) {
        // Implementação básica do jQuery
        // Em produção, use o arquivo completo baixado do site oficial
        return {
            ready: function(callback) { callback(); },
            on: function() { return this; },
            off: function() { return this; },
            click: function() { return this; },
            submit: function() { return this; },
            val: function() { return ''; },
            html: function() { return this; },
            text: function() { return this; },
            addClass: function() { return this; },
            removeClass: function() { return this; },
            toggleClass: function() { return this; },
            show: function() { return this; },
            hide: function() { return this; },
            fadeIn: function() { return this; },
            fadeOut: function() { return this; },
            slideDown: function() { return this; },
            slideUp: function() { return this; },
            ajax: function() { return this; },
            get: function() { return this; },
            post: function() { return this; }
        };
    };
    
    // Adicionar métodos estáticos
    jQuery.ajax = function() {};
    jQuery.get = function() {};
    jQuery.post = function() {};
    jQuery.ready = function(callback) { callback(); };
    
    console.log('jQuery carregado localmente. Para funcionalidade completa, baixe o arquivo oficial.');
}
